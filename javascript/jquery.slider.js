/*global jQuery*/

(function ($) {
    'use strict';

    var Slider;

    Slider = (function () {
        return {
            init: function () {
            },
            next: function () {
            },
            previous: function () {
            }
        };
    }());

    $.fn.slider = function () {
        return this.each(function () {
        });
    };
}(jQuery));